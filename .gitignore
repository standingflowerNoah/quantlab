# ── 数据与运行时产物（体积大/每日可再生，不入库）──
# 仅根目录数据湖；不带前导斜杠会连 quantlab/data/ 源码包一起忽略
/data/
logs/
reports/
portfolio_state/
.workbuddy/

# ── 第三方工具链（free-stockdb，24GB 含镜像 zip，另行管理）──
tools/

# ── Python 运行时 ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ipynb_checkpoints/

# ── 数据库与缓存 ──
*.duckdb
*.duckdb.wal
*.pkl

# ── 编辑器/系统 ──
.DS_Store
Thumbs.db

# ── 临时/工具缓存 ──
.playwright-cli/
csi_search_tmp.json
