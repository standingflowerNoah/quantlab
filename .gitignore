# ── 数据与运行时产物（体积大/每日可再生，不入库）──
# 仅根目录数据湖；不带前导斜杠会连 quantlab/data/ 源码包一起忽略
/data/
logs/
reports/
portfolio_state/
.workbuddy/

# ── 第三方工具链（free-stockdb，24GB 含镜像 zip，另行管理）──
tools/

# ── Python 运行时 ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ipynb_checkpoints/

# ── 数据库与缓存 ──
*.duckdb
*.duckdb.wal
*.pkl

# ── 编辑器/系统 ──
.DS_Store
Thumbs.db

# ── 临时/工具缓存 ──
.playwright-cli/
csi_search_tmp.json
.tmp/

# ── 生成型看板/报告（可由脚本再生，云端有镜像，不入库）──
/docs/factor_center_*.html
/docs/S1s*.html

# ── 会话级一次性运维脚本（下划线前缀 = 临时）──
/scripts/_*
/scripts/run_postclose_*.bat
/scripts/wait_and_check_*.sh
/scripts/factor_eval/_*.json

# ── PandaAI 抓取实验产物（panda_pull/ 脚本入库，站点 JS/输出不入库）──
/panda_api_test.json
/panda_*.html
/panda_js/

# 密钥（API token 等）绝不入库
.secrets/
